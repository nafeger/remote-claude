# Slack Bot Configuration
# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# App-Level Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Configuration Directory
# Default: ~/.remote-claude
# This directory will contain:
# - config.json (channel-project mappings)
# - snippets.json (prompt snippets)
# - state.json (session states)
CONFIG_DIR=~/.remote-claude

# Logging Configuration
# Valid values: debug, info, warn, error
# Default: info
LOG_LEVEL=info

# tmux Output Configuration
# Number of lines to capture from tmux pane by default
# Valid range: 10-200
# Default: 50
DEFAULT_OUTPUT_LINES=50
